{
  "name": "coderabbit-advanced-poc",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "services/*",
    "libs/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/jest": "^29.5.14",
    "@types/node": "^24.4.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.1",
    "ts-node-dev": "^2.0.0"
  }
}